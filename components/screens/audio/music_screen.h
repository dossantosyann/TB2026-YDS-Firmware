/**
 * @file music_screen.h
 * @brief The Music menu (top-level), reached from the home screen. Stub for now.
 */
#pragma once

#include "screen.h"

/**
 * @brief Get the singleton Music screen, ready to push onto the navigator.
 * @return Pointer to the Music screen.
 */
screen_t *music_screen(void);
